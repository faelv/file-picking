unit Fp.Resources.Strings;

interface

resourcestring

  RS_MSG_NOT_MIN_OS = 'This application can run only on Windows XP or above.';
  RS_MSG_ANOTHER_INSTANCE = 'Another instance of this application is already running.';
  LOAD_LANG_FAIL = 'An attempt to load the language file "%s" failed with the following errors:' + #13#10 + '%s';

implementation

end.
